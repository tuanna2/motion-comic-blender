[project]
name = "motion-comic-blender"
version = "0.1.0"
description = "JSON-driven deterministic 2D/2.5D motion comic renderer for Blender"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100

